# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Inference (torch installed separately via setup script — see scripts/setup.ps1)
transformers>=4.45.0
peft>=0.19.0
safetensors>=0.4.0
accelerate>=0.33.0
bitsandbytes>=0.43.0

# Image
Pillow>=10.4.0

# Rule engine
rapidfuzz>=3.9.0

# Encryption
cryptography>=43.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.8.0

# PDF parsing (setup scripts only)
pdfplumber>=0.11.0
